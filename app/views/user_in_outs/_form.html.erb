<%= form_with(model: user_in_out, local: true) do |form| %>
  <% if user_in_out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_in_out.errors.count, "error") %> prohibited this user_in_out from being saved:</h2>

      <ul>
      <% user_in_out.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_in %>
    <%= form.time_select :user_in, value: DateTime.now.in_time_zone.strftime("%F at %I:%M%p") %>
  </div>

  <div class="field">
    <%= form.label :user_out %>
    <%= form.time_select :user_out, value: DateTime.now.in_time_zone.strftime("%F at %I:%M%p") %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
